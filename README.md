# Get started with Edgeless DB

This repository contains a brief introduction to Edgeless DB and its core concepts in `getting-started.md` and a corresponding Docker Compose file in `docker-compose.yml`.
